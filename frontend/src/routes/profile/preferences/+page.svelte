<script lang="ts">import { preferences } from '$lib/stores/preferences'; import { getActiveBasemaps } from '$lib/components/map/basemaps'; const layers=['tanah','bangunan','jalan','saluran','lapangan','makam','taman','lainnya'];</script>

<svelte:head><title>SIMANTA - Preferensi Profil</title></svelte:head><div class="space-y-4"><h1 class="text-2xl font-bold">Preferensi Profil</h1><div class="card space-y-4"><label>Theme<select class="input mt-1" bind:value={$preferences.theme}><option value="light">Light</option><option value="dark">Dark</option><option value="system">System</option></select></label><label>Default basemap<select class="input mt-1" bind:value={$preferences.defaultBasemap}>{#each getActiveBasemaps() as provider}<option value={provider.key}>{provider.name}</option>{/each}</select></label><div><div class="font-semibold">Layer default</div>{#each layers as l}<label class="mr-3 inline-flex items-center gap-1 text-sm"><input type="checkbox" bind:group={$preferences.visibleLayers} value={l}/> {l}</label>{/each}</div><p class="text-sm text-slate-500">Persistensi MVP memakai localStorage; produksi memakai /api/v1/prefs.</p></div><div class="grid gap-4 md:grid-cols-2"><div class="card"><h2 class="font-semibold">Backup codes</h2><p>Status placeholder: belum dibuat.</p></div><div class="card"><h2 class="font-semibold">Active sessions</h2><p>Placeholder sesi aktif dan refresh token rotation.</p></div></div></div>
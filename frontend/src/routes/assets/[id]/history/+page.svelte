<script lang="ts">import { page } from '$app/stores'; import { onMount } from 'svelte'; import { getAssetHistory } from '$lib/services/api/assets'; let history:any[]=[]; onMount(async()=>{const r=await getAssetHistory($page.params.id as string); if(r.success) history=r.data});</script><div class="space-y-4"><h1 class="text-2xl font-bold">Riwayat Aset</h1><div class="card">{#each history as h}<div class="border-l-4 border-emerald-500 py-3 pl-4"><b>{h.action}</b> • {h.at}<div>{h.summary}</div><div class="text-xs text-slate-500">Actor: {h.actor}</div></div>{/each}</div></div>